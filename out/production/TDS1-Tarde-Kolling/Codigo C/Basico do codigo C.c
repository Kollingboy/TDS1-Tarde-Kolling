/*
O QUE É CODIGO C
A linguagem C é chamada de linguagem nível intermediária, pois combina os elementos das linguagens de alto nível com o funcionalismo da linguagem nível máquina.
Com C é possível ter controle exato de como um programa se comporta e dá acesso direto à partes do hardware, como o espaço na memória do computado
-//-//-//-//-//-
SINTAXE
* Ordens ao pré-processador, conhecidas como diretivas do pré-processador. Normalmente, incluem
arquivos de cabeçalho e definição de constantes.
* Declarações de variáveis e funções visíveis em todo o arquivo. Em outras palavras, os nomes dessas
variáveis e funções podem ser utilizados em qualquer função desse arquivo. Caso se deseje limitar a
visibilidade das variáveis e funções somente a esse módulo, deve ser colocado diante de seus nomes o
prefixo static; caso contrário, a palavra reservada extern indica que os elementos são declarados e
definidos em outro arquivo.
* O resto do arquivo inclui definições das funções (seu corpo). Dentro de um corpo de uma função, podem
ser definidas variáveis que são locais à função e que somente existem no código da função que se está
executando
-//-//-//-//-//-
VARIÁVEIS

Em C temos os seguintes tipos básicos de variáveis:
char: Guarda um caracter;
• int: Guarda um número inteiro;
• float: Guarda um número real com certa precisão;
• double: Guarda um número real com precisão maior que float;
• void: Tipo vazio.
-//-//-//-//-//-
*/
